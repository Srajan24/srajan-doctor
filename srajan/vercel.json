{
  "buildCommand": "cd backend && npm install",
  "outputDirectory": "backend",
  "installCommand": "cd backend && npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/backend"
    }
  ]
}
